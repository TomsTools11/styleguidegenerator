# Redis Configuration
# Railway provides REDIS_URL automatically when you add a Redis service
# For local development, use a local Redis instance
REDIS_URL=redis://localhost:6379

# Job Configuration
# Time-to-live for jobs in seconds (default: 24 hours)
# Jobs auto-expire after this duration to prevent memory leaks
JOB_TTL_SECONDS=86400
